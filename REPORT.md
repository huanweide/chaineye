# chaineye 工作单元报告（费曼版）

> 写给零基础读者的「我到底做了什么、怎么做的、踩了哪些坑」。

## 一句话结论
做了一个命令行小工具 **chaineye**，帮你一秒搞清楚：**「我改了一个代码文件，会牵连哪些其他文件？」** 已上架 GitHub，可直接 `npx chaineye .` 使用，也能当 CI 质量闸门挡住循环依赖。

---

## 干这件事像干什么（通俗类比）
把代码仓库想成一张**蜘蛛网**：每个文件是一个节点，`import` 语句就是节点之间的丝。你扯动其中一个节点（改文件），网上哪些点会跟着晃（受影响）？手动 `grep` 梳理费劲又容易漏。chaineye 就是一张「自动找出会晃的节点」的网图仪。

---

## 为什么这么做（底层原理）
- **痛点真实**：每次进陌生仓库，或改一个底层文件前，最怕漏掉上游调用方导致连锁崩。这是高频刚需。
- **第一性原理取舍**：现有工具（madge / dependency-cruiser）能做图，但配置重、功能巨多、CI 门槛要自己拼。我**不做全功能平台**，只把「影响范围」这一件事做到极致——零配置、确定性、可进 CI。这是典型的「巨头嫌小、现有开源嫌重」缝隙打法。

---

## 怎么做的（方法 / 工具 / 效果）

**工具**：纯 Node.js 单文件，**零第三方依赖**（只用电脑自带的文件/路径模块），`git clone` 即可跑，无 `node_modules`。

**四步流水线**：
1. **扫描**：递归遍历仓库，跳过 `node_modules`/`.git`/`dist` 等，收集 JS/TS/Python/Go 源文件。
2. **解析**：用正则把每个文件里的相对导入（`import` / `require` / Python `from .x` / Go `import "./x"`）抽出来，自动补扩展名、目录索引，解析成仓库内真实文件路径。
3. **建图**：画一张「谁依赖谁」的有向图（A→B 表示 A 依赖 B），同时存一份反向边。
4. **分析**：
   - **影响范围** = 从你改的文件出发，沿反向边做传递闭包（所有会因它变更而受影响的文件）。
   - **循环依赖** = 在图上找环（谁依赖谁又绕回来）。
   - **孤立文件** = 既没人依赖、也不依赖别人的文件。
   - **枢纽文件** = 被依赖最多的文件（改它影响面最大）。

**效果**：22 个自动化测试全过；在真实仓库跑通全部命令；`impact` 真实识别出 `index.js` 改动会波及 `test.js`。

---

## 关键取舍与踩坑（真实复现，照做可避）

1. **Python 相对导入认不出**（中坑）
   - 现象：`from .f import h` 里的 `.f` 是模块名，不是路径，直接当路径解析会找不到依赖。
   - 修复：把前导点转成层级（`.`=当前目录、`..`=上一级），剩余的点换成斜杠 → `.f` 变成 `./f`，再补 `.py` 扩展名。
   - 复现命令：`node test.js` 里「parseImports 解析 Python 相对导入」这条就是验证。

2. **找循环依赖会栈溢出**（高危）
   - 现象：原本用「递归」深度优先搜索找环，仓库依赖链上万层时直接 `RangeError` 崩溃。
   - 修复：改成**显式栈迭代**（自己用数组当栈，三色标记），彻底规避递归深度限制。
   - 复现：`node -e` 构造 15000 文件链跑 `findCycleNodes` 不再崩。

3. **CI 门禁被静默放行**（中危）
   - 现象：`--max-cycles abc` 解析成 `NaN`，而 `数量 > NaN` 永远为假，门禁形同虚设。
   - 修复：解析后必须 `Number.isFinite` 校验，非整数直接 `exit 2` 报错。
   - 复现：`node index.js . --max-cycles abc` 现在退出码 2（原来 0 通过）。

4. **错误路径谎报通过**（中危）
   - 现象：`chaineye /不存在的路径` 返回 0 文件、还报「CI 门禁通过」。
   - 修复：扫描前先用 `statSync` 校验根目录存在且是目录，否则 `exit 2`。

5. **`--help` 失效**（体验阻塞）
   - 现象：`--help` 被当成目录名，静默扫不存在的目录后谎报通过。
   - 修复：解析阶段命中 `--help`/`-h` 直接打印用法并退出 0。

---

## 交付物
- GitHub 公开仓库：https://github.com/huanweide/chaineye
- 本地目录：`forge/projects/2026-08-13/ol-chaineye/`（含 index.js / test.js / README.md / selection.md）
- 跨项目学习已写入 `core-memory.md`（项目沉淀 + CLI 门禁参数校验铁律）。

## 下一步还能做（未做，留口子）
- 符号链接跟随（目前会跳过）、更多语言（Rust/C#）、把影响范围画成可视化 SVG 图。
